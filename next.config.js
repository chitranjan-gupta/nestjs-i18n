// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  i18n: {
    locales: ["en-US", "en-IN", "en-UK", "nl-NL"],
    defaultLocale: "en-US",
  },
};

module.exports = nextConfig;
